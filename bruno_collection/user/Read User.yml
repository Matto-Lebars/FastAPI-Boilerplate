info:
  name: Read User
  type: http
  seq: 4

http:
  method: GET
  url: http://{{ip}}/api/v1/user/{{queryusername}}
  auth: inherit

runtime:
  variables:
    - name: queryusername
      value: userson

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
